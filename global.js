module.exports = {
	// Base url
	base_url : '/',
	// Base directory
	base_dir : __dirname,
	// App Directory
	app_dir : __dirname + "/app",
	// Api logic directory
	app_logic_dir : __dirname + "/app/logic",
	// Api model directory
	app_model_dir : __dirname + "/app/model",
	// Api middleware directory
	app_middleware_dir : __dirname + "/app/middleware",
	// Api storage directory
	app_storage_dir : __dirname + "/app/storage",
	// Your global variable goes below
	// ...
}